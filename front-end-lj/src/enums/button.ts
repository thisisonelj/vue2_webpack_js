export enum buttonSize {
  SMALL = 'small',
  DEFAULT = 'default',
  LARGE = 'large',
}

export enum buttonType {
  PRIMARY = 'primary',
  SUCCESS = 'success',
  WARNING = 'warning',
  DANGER = 'danger',
  INFO = 'info',
}
